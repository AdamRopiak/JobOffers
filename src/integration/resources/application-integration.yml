joboffers:
  jobfetcher:
    scheduler:
      joboffers:
        delay: PT1S
    http:
      client:
        config:
          uri: http://localhost
          connectionTimeout: 1000
          readTimeout: 1000
scheduling:
  enabled: false